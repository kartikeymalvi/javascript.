var age=45;
 console.log(typeof age);
 var newAge =age.toString();
  console.log(typeof newAge);

  var data="javascript";
   var text ="it is a dynamic language";
     var combined= data.concat(text);
   // var combined= ` ${data} ${text}`;
      console.log(combined);
       // returns the numbers of charcters in a string
   var  dataLen = data.length;

       console.log(dataLen);
       // charAt() - returns the caracters at spacific index number
         var newChar= data.charAt(5);
          console.log(newChar);
           
             var uppercase= data.toUpperCase();
              var lowercase=data.toLowerCase();
                console.log(uppercase);
                console.log(lowercase);
                 // returns the code of that character
                  var 